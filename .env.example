PORT=3000
MONGODB_URI=mongodb://localhost:27017/merkadapp_expenses
NODE_ENV=development

# Comma-separated list of frontend origins allowed to call this API with credentials
CORS_ORIGINS=http://localhost:3001

# From the Firebase service account JSON (Project settings → Service accounts)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=